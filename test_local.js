const fs = require('fs'); const content = fs.readFileSync('server/routes/sites.js', 'utf8'); const match = content.match(/function getFallbackHtml\(sName, sDesc\) \{[\s\S]*?\}\s*const fallbackHtml/); if(match) { const func = match[0].replace('const fallbackHtml', ''); eval(func); const html = getFallbackHtml('Imobiliaria Vermelha', 'quero um site de imobiliaria super moderno, na cor vermelho e claro'); fs.writeFileSync('public/test-site.html', html); console.log('Primary color from HTML:', html.match(/--primary:\s*([^;]+);/)[1]); console.log('Light color from HTML:', html.match(/--light:\s*([^;]+);/)[1]); }
